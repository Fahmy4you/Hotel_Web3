import React from 'react'
import TableUI from '@/components/TableUI'
const page = () => {
  return (
    <div>
        <TableUI onAddUser={() => {}} onSearch={() => {}} />
    </div>
  )
}

export default page